[package]
name = "tessera-bridge"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "tessera_bridge"
crate-type = ["cdylib"]

[dependencies]
jni = "0.21.1"
blake3 = "1.8.6"

[build-dependencies]
cc = "1.4.0"

[profile.release]
lto = true
panic = "abort"
